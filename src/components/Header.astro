---

import SidebarButton from './SidebarButton.astro';
import SidebarToggleButton from './react/SidebarToggleButton.tsx';

const props = Astro.props

---

<div id="header" class="fixed h-[50px] bg-[#222225] w-full z-50 bg-opacity-50 backdrop-blur-sm">

	<SidebarToggleButton client:only />

	<p class="font-heading font-bold text-2xl mt-2 mr-10 ml-auto text-right w-fit text-white float-right invisible md:visible" title="Page Title">{props.pageName}</p>

</div>



<!-- <div id="header-spacer" class="h-[50px] mb-10"/> -->

<div id="sidebar" class="block fixed h-[calc(100%-50px)] top-[50px] w-[100%] -left-[100%] md:w-[15rem] md:left-[-15rem]  m-0  md:rounded-br-2xl md:shadow-2xl z-50 bg-[#222225] bg-opacity-50 backdrop-blur-sm">

	<SidebarButton title="Home" symbol="home" link="/"/>
	<SidebarButton title="About" symbol="person_book" link="/about" />
	<!--<SidebarButton title="Achievements" symbol="trophy" link="achievements" />
	<SidebarButton title="Gallery" symbol="photo_library" link="gallery" />
	<SidebarButton title="Blog" symbol="wallpaper_slideshow" link="blog" />-->
	<SidebarButton title="Links" symbol="link" link="/linkinbio" />

</div>

<script>

	import { animate, scroll } from "motion"

	animate("#header", { y: [-50, 0], opacity: [0, 100] }, { ease: "easeInOut", duration: 1} )

</script>